///scr_player_attack_draw()

/**
 * Player Attack: On Draw
 *
 * Added to the player attack object's Draw event.
 */

// draw the object's sprite
draw_self();

/*
if (DEBUG_MODE)
{
    // draw bounding box
    draw_set_colour(c_red);
    draw_rectangle(bbox_left, bbox_top, bbox_right, bbox_bottom, true);
}
*/

